

language: cpp
dist: xenial
sudo: false

addons:
  apt:
    packages:
      - python3
      - python3-pip
      - python3-setuptools

before_install:
  - which pip3
  - pip3 install numpy matplotlib

script:
  - pip3 freeze
